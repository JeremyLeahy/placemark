<table class="table is-fullwidth">
    <thead>
    <tr>
        <th>Description</th>

    </tr>
    </thead>
    <tbody>
    {{#each coffeeShop.details}}
        <tr>
            <td>
                {{title}}
            </td>
            <a href="/coffeeshop/{{../coffeeShop._id}}/deleteinfo/{{_id}}" class="ui icon button">
                <i class="fas fa-trash"></i>
            </a>
        </tr>
    {{/each}}
    </tbody>
</table>